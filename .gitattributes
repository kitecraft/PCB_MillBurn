* text=auto

*.cs        text diff=csharp
*.axaml     text
*.xaml      text
*.csproj    text
*.props     text
*.slnx      text
*.md        text
*.yml       text
*.json      text

# Shell scripts run under bash on every platform, and bash reads a carriage return as part of the
# command. LF everywhere, including a Windows checkout.
*.sh        text eol=lf

# Gerber / Excellon / G-code corpus files: keep bytes exactly as supplied, since golden tests
# compare them and some tools are sensitive to line endings.
*.gbr       -text
*.ger       -text
*.drl       -text
*.txt       -text
*.nc        -text
*.ngc       -text

*.ico       binary
*.png       binary
*.pdf       binary
*.ttf       binary
